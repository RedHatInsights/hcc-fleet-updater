---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hccappconfigurations.hcc.redhat.com
spec:
  group: hcc.redhat.com
  names:
    kind: HCCAppConfiguration
    listKind: HCCAppConfigurationList
    plural: hccappconfigurations
    singular: hccappconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clowdAppName
      name: ClowdApp
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.healthy
      name: Healthy
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HCCAppConfiguration is the Schema for the hccappconfigurations API.
          It provides persistent inventory and health tracking for a ClowdApp.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HCCAppConfigurationSpec defines the desired state of HCCAppConfiguration.
            properties:
              clowdAppName:
                description: clowdAppName is the metadata.name of the ClowdApp CR
                  to monitor.
                minLength: 1
                type: string
              enabled:
                default: true
                description: enabled controls whether this app configuration is actively
                  monitored.
                type: boolean
              images:
                description: images lists the image repositories this app uses.
                items:
                  description: ImageRegistryEntry identifies an image repository that
                    an app uses.
                  properties:
                    image:
                      description: image is the full container image repository path
                        (e.g. quay.io/cloudservices/advisor-backend).
                      minLength: 1
                      type: string
                  required:
                  - image
                  type: object
                type: array
              namespace:
                description: namespace is the namespace of the ClowdApp. Defaults
                  to the operator namespace.
                type: string
            required:
            - clowdAppName
            type: object
          status:
            description: HCCAppConfigurationStatus defines the observed state of HCCAppConfiguration.
            properties:
              conditions:
                description: conditions represent the current state of the HCCAppConfiguration.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              currentImages:
                description: currentImages reflects the image:tag pairs currently
                  running on the ClowdApp.
                items:
                  description: ImageTagPair specifies a container image and its desired
                    tag.
                  properties:
                    image:
                      description: image is the full container image repository path
                        (e.g. quay.io/cloudservices/advisor-backend).
                      minLength: 1
                      type: string
                    tag:
                      description: tag is the desired image tag (e.g. a1b2c3d).
                      minLength: 1
                      type: string
                  required:
                  - image
                  - tag
                  type: object
                type: array
              healthy:
                description: healthy indicates whether the ClowdApp's deployments
                  are healthy.
                type: boolean
              lastUpdated:
                description: lastUpdated is when the status was last refreshed.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
